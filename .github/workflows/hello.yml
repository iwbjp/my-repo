name: Hello
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello, world"
      - run: |
          git log -1
          tree -a .
